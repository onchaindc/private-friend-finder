# Private Friend Finder Standalone Demo

This folder contains the demo-ready app that can be deployed as static files.

Deploy settings:

- Netlify publish directory: `demo`
- Vercel output directory: `demo`
- Build command: leave empty
- Install command: leave empty

Open `index.html` locally or deploy this folder directly.
